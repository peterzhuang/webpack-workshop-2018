// take a string, the button lable and return a element

// module.exports = (buttonName) => {
//     return `Button: ${buttonName}`;
// };
/**
 * 
 * @param {string} buttonName 
 * @returns {Element}
 */
const makeButton = buttonName => {
    return `Button: ${buttonName}`;
};

export default makeButton;
